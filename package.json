{
  "name": "remote-vm-runner",
  "version": "1.0.0",
  "description": "A robust TypeScript script for connecting to remote virtual machines via SSH and executing commands.",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ssh2": "^1.5.0"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/ssh2": "^1.15.5",
    "minimist": "^1.2.8",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
